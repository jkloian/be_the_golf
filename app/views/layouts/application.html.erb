<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Be The Golf" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Be The Golf">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <% if Rails.env.test? %>
      <%# In test mode, use hardcoded asset paths from vite build %>
      <script type="module" src="/vite-test/assets/application-4vSw5HRt.js"></script>
      <link rel="stylesheet" href="/vite-test/assets/application-Czdb3Elx.css">
    <% else %>
      <%= vite_client_tag %>
      <%= vite_javascript_tag 'entrypoints/application.tsx' %>
    <% end %>
  </head>

  <body>
    <div id="app"></div>
    <%= yield %>
  </body>
</html>
