module.exports = {
  plugins: {
    // Tailwind CSS is handled by @tailwindcss/vite plugin in vite.config.ts
    // Only autoprefixer is needed here for PostCSS processing
    autoprefixer: {},
  },
}
