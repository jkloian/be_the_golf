#!/usr/bin/env ruby
require "fileutils"
require 'dotenv'

Dotenv.load('.env.development')
puts "------: " + ENV["PORT"]
# Ensure all needed build directories exist
FileUtils.mkdir_p "app/assets/builds"

# Execute foreman with our Procfile.dev
exec "bundle exec foreman start -f Procfile.dev"

