#!/usr/bin/env bash
# Optional: trim public/vite and public/images before docker build when assets are served from S3
# and the image is built with SKIP_VITE_BUILD=true. Not used by default .release.yml in this repo.
set -euo pipefail
cd "$(dirname "$0")/.."
rm -rf public/images public/vite/assets
