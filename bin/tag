#!/usr/bin/env ruby
# frozen_string_literal: true

APP_ROOT = File.expand_path("..", __dir__)
Dir.chdir(APP_ROOT) do
  exec({ "BUNDLE_GEMFILE" => File.join(APP_ROOT, "Gemfile") }, "bundle", "exec", "git-docker-release", "tag", *ARGV)
end
